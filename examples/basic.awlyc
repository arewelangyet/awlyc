# import parsing "parsing.awlyc"
# import interning "interning.awlyc"

fn topic(id, name, description) {
  {
    id,
    name,
    description,
  }
}

fn project(name, repo, crates_io, description, docs, topics) {
  {
    name,
    repo,
    crates_io,
    description,
    docs,
    topics,
  }
}

{
  title: "Hi",
  author: "Test"
}